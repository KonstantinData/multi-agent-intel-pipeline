# Tasks

Task definitions are concrete execution units for this repository.

Each task should include:
- scope classification (`security_pipeline`, `runtime_workflow`, or `both`)
- file/path triggers
- canonical commands
- measurable acceptance criteria

Task definitions under `tasks/` are the source of truth for "real tasks".
