# Config

Defines assistant operating profiles and routing defaults.

- `profiles/`: task-depth profiles (`fast`, `standard`, `deep`).
- `routing/`: model/tool routing policies by task type and risk.
