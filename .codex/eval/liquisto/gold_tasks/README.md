# Gold Tasks

Keep a small, stable benchmark set that represents common high-value workflows:
- contract-safe changes
- supervisor boundary-sensitive changes
- follow-up routing and retrieval scenarios
- architecture-test-layer tasks

Each task definition should include target files, risk level, and expected validation scope.
