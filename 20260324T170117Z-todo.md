# Run 20260324T170117Z — TODO

**Company:** ZF Friedrichshafen AG · zf.com
**Status:** `completed_partial` — 4 accepted, 4 rejected, 4 degraded
**Laufzeit:** 421 s · **Kosten:** $0.018 · **Modell:** gpt-4.1-mini only

---

## P0 — LLM-Synthesis-Mapping (Hauptursache für 8/12 nicht-accepted Tasks)

- [x] **Economic Situation nicht ins Payload gemappt**
  Worker hat Fakten gesammelt (Umsatz −11%, EBIT 3,6%, Restrukturierung Division E, 7.600 Stellen),
  aber `economic_situation.revenue_trend`, `.profitability`, `.recent_events`, `.inventory_signals`, `.assessment`
  blieben alle auf `n/v` / `[]`.
  → `_llm_synthesis` oder `_normalize_payload_updates` in `src/agents/worker.py` muss verschachtelte
  Objekte (`economic_situation.*`) zuverlässig aus den gesammelten Fakten befüllen.
  ✅ Fix: Neuer Prompt-Block für `economic_commercial_situation` in `_llm_synthesis` schreibt alle 6 Felder explizit vor.

- [x] **Market Situation: Fakten vorhanden, Payload leer**
  Quellen (S&P Global, McKinsey, PwC, ZF-Pressemitteilungen) gefetcht, Signale extrahiert,
  aber `key_trends`, `demand_outlook`, `assessment` blieben `n/v` / `[]`.
  → Gleiche Ursache wie oben. Fallback-Merge aus `market_signals` → Schema-Felder einbauen.
  ✅ Fix: Bridge in `run()` nach LLM-Synthesis: `market_signals` → `key_trends`, `facts` → `demand_outlook`, auto-`assessment` aus `key_trends`.

- [x] **Contact Discovery: 3 Kontakte in Facts, 0 im Payload**
  Dr. Arne Flemming (SVP Supply Chain, Bosch), Djalma Goncalves (Dir. Procurement, Bosch),
  Volker Stitz (VP Purchasing, Bosch) sind in `facts` + `sources` vorhanden,
  aber `contact_intelligence.contacts` = `[]`, `contacts_found` = 0.
  → Bridge-Logik: Kontakte aus Facts/Sources ins ContactIntelligenceSection-Schema überführen,
  wenn LLM-Synthesis das nicht schafft.
  ✅ Fix: Bridge in `run()` — wenn `contacts == []` nach LLM-Synthesis, werden Search-Result-Titel per `_parse_contact_from_title` geparst und ins Schema überführt.

## P1 — Revision-Schleife wirkungslos

- [x] **Revision bringt keine Verbesserung**
  Bei keinem rejected/degraded Task hat die Critic → Worker Retry-Schleife die Lücken geschlossen.
  Gleiche `n/v`-Felder vor und nach Revision.
  → Revision-Instructions spezifischer machen: bereits gesammelte Fakten als Kontext mitgeben,
  statt nur "Rework the missing or weak point 'X'".
  → Prüfen ob der Worker bei Revision tatsächlich neue Queries/Fetches macht oder nur den
  gleichen Evidence-Pack erneut durch die LLM-Synthesis schickt.
  ✅ Fix: Revision-Prompt injiziert jetzt `current_section`-Stand ("fill MISSING fields, keep existing ones") als konkreten Kontext in `_llm_synthesis`.

## P2 — Buyer Department: Zu wenig Peers

- [x] **Nur 1 Peer (Robert Bosch GmbH) für einen €41 Mrd. Tier-1-Zulieferer**
  Offensichtliche Peers fehlen: Continental, Magna, Schaeffler, Aisin, Denso, ZF Aftermarket-Wettbewerber.
  → Query-Strategie in `build_buyer_queries` erweitern: generische Tier-1-Queries
  ("top automotive Tier 1 suppliers global", "automotive component manufacturers Europe ranking").
  → `peer_competitors.assessment` war leer → Critic hat rejected, Revision hat nichts gebracht.
  ✅ Fix: `_build_queries` für `peer_companies` hat jetzt 4 dedizierte Queries inkl. "top Automotive Tier 1 suppliers global ranking" und "Automotive component manufacturers global competitors ranking".

- [x] **Keine Downstream-Buyers, Monetization-Paths, Redeployment-Paths**
  Alle drei Listen leer trotz vorhandener Buyer-Hypothesen in `buyer_hypotheses`
  (Aftermarket-Distributoren, Fleet-Operators, OEM-Lizenzierung).
  → Gleiche Mapping-Lücke wie P0, aber im MarketNetwork-Schema.
  ✅ Fix: Neuer Prompt-Block für `monetization_redeployment` in `_llm_synthesis` + Bridge: wenn `downstream_buyers.companies == []` nach Synthesis, werden `buyer_hypotheses` ins Schema überführt; `monetization_paths` und `redeployment_paths` analog aus `buyer_hypotheses`/`facts`.

## P3 — Fehlende Sektionen

- [x] **Product Asset Scope: Rejected, keine Güterklassifikation**
  `product_asset_scope` = `[]`. Nur generisches `goods_classification` = `manufacturer`.
  Keine Unterscheidung made vs. distributed vs. held-in-stock.
  → Prüfen ob der Worker für `product_asset_scope` überhaupt spezifische Queries baut
  (z.B. "ZF Friedrichshafen spare parts inventory", "ZF aftermarket product catalog").
  ✅ Fix: Neuer Prompt-Block für `product_asset_scope` in `_llm_synthesis` — fordert konkrete Produktfamilien mit Vermarktungstyp (manufactured/distributed/held-in-stock) und `goods_classification`.

- [x] **Repurposing/Circularity: Rejected, keine Signale**
  `repurposing_signals` = `[]` trotz Queries zu Recycling/Reuse.
  → Möglicherweise keine öffentlichen Daten verfügbar. Prüfen ob der Worker
  die ZF-Nachhaltigkeitsberichte (CDP-Report ist in Sources) tatsächlich auswertet.
  ✅ Fix: Prompt für `repurposing_circularity` explizit auf CDP/Sustainability-Reports erweitert + Inferenz aus Restrukturierungsnews erlaubt. Bridge: wenn `repurposing_signals == []` nach Synthesis, werden produkt-keyword-basierte Indikatoren als Fallback generiert.

## P4 — Optimierungen

- [x] **Nur gpt-4.1-mini verwendet**
  Laut Architektur sollten Lead/Critic/Judge gpt-4.1 nutzen, Researcher gpt-4.1-mini.
  Dieser Run hat ausschließlich gpt-4.1-mini für alle 8 LLM-Calls verwendet.
  → Prüfen ob `ROLE_MODEL_DEFAULTS` in `src/config/settings.py` korrekt konfiguriert ist
  und ob die Department-Runtime die Modell-Zuweisung pro Rolle durchreicht.
  ✅ Verifiziert: Critic/Judge/Lead sind regelbasiert (kein LLM-Call). Alle 8 LLM-Calls sind ausschließlich Researcher-Calls — `gpt-4.1-mini` per `ROLE_STRUCTURED_MODEL_DEFAULTS` ist korrekt. Kein Code-Change nötig.

- [x] **Budget-Auslastung sehr niedrig**
  39/140 Tool-Calls (28%), 8 LLM-Calls, $0.018.
  → Die Pipeline bricht zu früh ab oder nutzt das verfügbare Budget nicht aus.
  Bei rejected Tasks könnte eine zweite Revision-Runde mit erweitertem Query-Set helfen.
  ✅ Fix: `MAX_TASK_RETRIES` in `src/config/settings.py` von 2 → 3 erhöht. Der Lead hat jetzt 2 Revision-Runden vor Judge-Eskalation statt 1. Wert bleibt via `LIQUISTO_MAX_TASK_RETRIES` env-var überschreibbar.

---

## Verifizierung nach Fix

Für jeden Fix oben den Run erneut ausführen und prüfen:

1. `economic_situation.*` Felder im CompanyProfile-Payload befüllt
2. `key_trends`, `demand_outlook`, `assessment` im IndustryAnalysis-Payload befüllt
3. `contacts` im ContactIntelligenceSection-Payload ≥ 1
4. `peer_competitors.companies` ≥ 3
5. `downstream_buyers.companies` ≥ 1
6. `monetization_paths` und `redeployment_paths` ≥ 1
7. `product_asset_scope` ≥ 1
8. Run-Status = `completed` statt `completed_partial`
